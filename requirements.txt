# LysoSense dependencies
# Runtime deps are pinned (==) so Streamlit Cloud builds the exact set validated
# locally; loose >= pins let a rebuild pull a newer release that broke the hosted app.
streamlit==1.52.2
pandas==2.3.3
numpy==2.3.5
plotly==6.5.0
scipy==1.16.3
openpyxl==3.1.5
scipy-stubs==1.17.0.0
pandas-stubs==2.3.3.251219
